# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment & secrets
.env
.env.*.local
server/.env
server/.env.local
serverBKP/.env
serverBKP/.env.local

# Logs
logs/
*.log
*.log.*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Build artifacts
dist/
build/
coverage/

# …but keep the Kokoro build tooling (not its node_modules) so the
# vendored bundle in Briefly/lib/vendor/ is reproducible.
!build/
!build/kokoro/
!build/kokoro/src/
!build/kokoro/*.mjs
!build/kokoro/package.json
build/kokoro/node_modules/
build/kokoro/package-lock.json

# Chrome extension build artifacts (if added later)
Briefly/*.zip
Briefly/dist/

# Legacy backend (optional)
serverBKP/node_modules/

