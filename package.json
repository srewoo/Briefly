{
  "name": "briefly-extension",
  "version": "3.6",
  "private": true,
  "type": "module",
  "description": "Two-way voice: read any page aloud (text-to-speech) and dictate text by voice (speech-to-text). Fast, private, in-browser.",
  "scripts": {
    "test": "node --test",
    "build": "rm -rf dist && mkdir -p dist && cd Briefly && zip -r ../dist/briefly.zip ."
  },
  "keywords": [
    "chrome-extension",
    "speech-to-text",
    "text-to-speech",
    "stt",
    "tts"
  ],
  "license": "MIT"
}